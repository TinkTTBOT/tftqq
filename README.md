# Dino TFT Builder (GitHub-ready package)

Upload toàn bộ nội dung này lên GitHub (giữ nguyên cấu trúc thư mục).  
Sau đó vào tab **Actions** → chạy workflow → tự động build APK.

File workflow nằm tại: `.github/workflows/build_apk.yml`
